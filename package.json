{
    "name": "fullstack-project",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "install-all": "npm install --prefix backend && npm install --prefix zoom_project",
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix zoom_project\"",
        "build": "npm run build --prefix zoom_project",
        "start": "concurrently \"npm run start --prefix backend\" \"npm run preview --prefix zoom_project\""
    },
    "dependencies": {
        "aos": "^2.3.4",
        "axios": "^1.8.4",
        "concurrently": "^8.0.0",
        "dotenv": "^16.5.0",
        "firebase": "^11.6.0",
        "form-data": "^4.0.2",
        "html2pdf.js": "^0.10.3",
        "openai": "^4.95.1",
        "react-type-animation": "^3.2.0"
    }
}
